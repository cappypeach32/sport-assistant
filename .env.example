# API-Football (required)
API_FOOTBALL_KEY=your_api_sports_key

# OpenAI — optional (GPT narratives / tactical copy)
OPENAI_API_KEY=your_openai_key

# App bind (internal — nginx proxies port 80 → APP_PORT)
APP_HOST=0.0.0.0
APP_PORT=8765

# Public URL shown in logs / docs (no trailing slash)
PUBLIC_APP_URL=http://212.227.188.126
